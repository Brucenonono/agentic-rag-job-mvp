# Tools

Future home of reusable tool interfaces.

Planned tool set:

- `parse_jd`
- `parse_resume`
- `retrieve_evidence`
- `score_match`
- `rewrite_resume`
- `generate_interview_prep`

Design note:

- We keep the interface MCP-style from the beginning.
- We do not commit to a distributed MCP deployment until the local pipeline is validated.
